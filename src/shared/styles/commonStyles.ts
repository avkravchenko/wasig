import { StyleSheet } from "react-native";

const commonStyles = StyleSheet.create({
  titleText: {
    fontSize: 18,
    fontWeight: "600",
    textAlign: "center",
    marginVertical: 16,
    letterSpacing: 0.5,
  },
});

export default commonStyles;
